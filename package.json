{
  "name": "api-one",
  "version": "0.0.1",
  "author": "Niek Janssen",
  "license": "MIT",
  "description": "Attempt at full automation of backand-synchronization and server backend storage. ",
  "homepage": "https://github.com/atasak/MusicOne#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/atasak/ApiOne"
  },
  "bugs": {
    "url": "https://github.com/atasak/ApiOne/issues"
  },
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "if exist dist\\ rd /s /q dist && tsc",
    "build:linux": "rm -rf dist && tsc",
    "build:travis": "tsc",
    "run": "node dist/compiler/runner.js",
    "test": "jasmine-ts",
    "doc": "typedoc --out doc src --exclude src/runtime/lib.bak/**/*",
    "prepare:temp": "rm -rf spec/common/gen spec/common/dist && mkdir spec/common/dist"
  },
  "dependencies": {
    "@types/node": "^8.5.2",
    "ts-simple-ast": "^4.2.1"
  },
  "devDependencies": {
    "@types/jasmine": "^2.8.0",
    "jasmine": "^2.8.0",
    "jasmine-spec-reporter": "^4.2.1",
    "jasmine-ts": "^0.2.1",
    "tslint": "^5.8.0"
  }
}
